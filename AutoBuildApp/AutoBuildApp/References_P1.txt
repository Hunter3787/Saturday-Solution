-- 12/1/2020

configuration manager:
	https://docs.microsoft.com/en-us/dotnet/api/system.configuration.configurationmanager.connectionstrings?view=dotnet-plat-ext-5.0

APP.CONFIG:
	connection string:
	https://www.connectionstrings.com/sql-server-2019/

	 providerName ="System.Data.SqlClient"/> -> this just says we are connected to SQL server
	 not excel or some other form

	 also note there can multiple connection strings





-- 12/2/2020



what are data base for now that we are working with looks like:
we are only working with a portion for testing !!!! FOR NOWWW
_______________________
USE Middleware_Testing

 CREATE TABLE userAccounts(

 userID INTEGER NOT NULL IDENTITY,
 username varchar(20),
 email varchar(20),
 passwordHash BINARY(64) NOT NULL,
 fName varchar(20),
 lName varchar(20),
 createdAt DATETIME,
 createdby Varchar(20),
 modifiedAt DATETIME,
 modifiedBy Varchar(20),
 -- creating a enum
 -- accountTypes represented as an enumeration:
 roley varchar(15) NOT NULL CHECK (roley IN ('BASIC' , 'ADMIN', 'DEVOLOPER', 'VENDOR')),

-- primary key
CONSTRAINT userAccounts_PK PRIMARY KEY(userID)
 );

 ____________ end.
REMEMBER NO APP SPECIFIC STUFF, JUST BASIC 
GENERIC IMPPLEMENTATIONS THAT YOUD DO IN A DATABASE 
CREATE() DELETE() RETRIEVE()
To do:
	create some DTOs 
		is Database Online?
		does user exist?
	some module DAL.something that comes from the service layer
	so: login Service -> calls -> login DAL
	for our case:
		Create User
		Delete user
		Retrieve user
		validate password 

	so the question is:
	Am i gana do it with a repository ?
		example:
						Crud repository:
					- A data access object that has these operations:
						Create
						Read
						Update
						Delete

				For example:
	
	
					User repository:
						Create user( String fname)
						GetUser(String fname)
						GetAllUsers()
						UpdateUser(String fname)
						DeleteUSer( String fname)
		

	or as a gateway?

		did he even talk about this?


----------------------stop here------------------

this brought up some good notes:


	https://thinkinginobjects.com/2012/08/26/dont-use-dao-use-repository/


	--------------------------------------------

	12/3/2020
	change of plans will be implementing data access gateway.
	http://whiteknight.github.io/2018/07/24/patterns.html

