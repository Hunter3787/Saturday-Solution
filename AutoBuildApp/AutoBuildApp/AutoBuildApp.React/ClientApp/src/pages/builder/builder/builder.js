import React from 'react';

/*
Table view with primary recommendation, in its own collumn,
and several alternatives sharing a collumn.
Recommended:
    carrosel of images of the components that make up the build.
    Changes label to match component.
Below two collumns is the components of the build in a list.
Component List:
    Show each component to match the selected build.
*/
function Builder() {
    return null;
}

export default Builder;