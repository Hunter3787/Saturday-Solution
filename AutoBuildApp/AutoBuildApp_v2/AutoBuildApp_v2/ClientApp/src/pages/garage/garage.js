import React from 'react';

/*
Shelf elemetn at the top of the page (with appropriate padding)
A dropdown to select which shelf will show.
Bottom section is a list of all saved builds.
    Builds will have an image item to the left of their details
    The details will be a descripription written in, 
    and the price will be the total estimate at the time of the build
    (possibly a second price showing its cost now)
    When a build is clicked on the card will expand
        Expanded card will show full build details as well as writeup.
*/
function Garage() {
    return null;
}

export default Garage;